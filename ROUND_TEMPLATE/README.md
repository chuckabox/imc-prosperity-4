# ROUND_TEMPLATE

Copy this folder to create a new round workspace:

1. Duplicate folder
2. Rename (example: `ROUND 3`)
3. Add your round files (data, traders, configs, tools)

This template intentionally starts empty but keeps the same high-level layout
as `ROUND 1` so the unified dashboard can discover and target it.
